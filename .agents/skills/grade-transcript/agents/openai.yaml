interface:
  display_name: "Grade Transcript"
  short_description: "Repo-specific patterns and workflows for grade-transcript"
  default_prompt: "Use the grade-transcript repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true